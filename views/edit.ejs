<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">

    <!-- Navbar -->
    <nav class="bg-indigo-600 text-white shadow-md">
        <div class="max-w-7xl mx-auto px-4 py-3 flex justify-between items-center">
            <h1 class="text-xl font-semibold">Panel de Administración</h1>
            <div class="flex space-x-4">
                <a href="/productos" class="hover:bg-indigo-500 px-3 py-2 rounded-md">Listado</a>
                <a href="/productos/add" class="hover:bg-indigo-500 px-3 py-2 rounded-md">Crear</a>
            </div>
        </div>
    </nav>

    <!-- Contenido -->
    <main class="max-w-3xl mx-auto bg-white mt-10 rounded-lg shadow-lg p-8">
        <h2 class="text-2xl font-bold text-gray-700 mb-6 text-center">Editar producto</h2>

        <form action="/producto/<%= producto.id %>/update" method="POST" enctype="multipart/form-data" class="space-y-5">

            <div>
                <label class="block font-semibold text-gray-700">Nombre</label>
                <input type="text" name="nombre" value="<%= producto.nombre %>" class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-indigo-400" required>
            </div>

            <div>
                <label class="block font-semibold text-gray-700">Marca</label>
                <input type="text" name="marca" value="<%= producto.marca %>" class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-indigo-400" required>
            </div>

            <div>
                <label class="block font-semibold text-gray-700">Descripción</label>
                <textarea name="descripcion" rows="3" class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-indigo-400" required><%= producto.descripcion %></textarea>
            </div>

            <div class="grid grid-cols-2 gap-4">
                <div>
                    <label class="block font-semibold text-gray-700">Precio (S/)</label>
                    <input type="number" step="0.01" name="precio" value="<%= producto.precio %>" class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-indigo-400" required>
                </div>

                <div>
                    <label class="block font-semibold text-gray-700">Stock</label>
                    <input type="number" name="stock" value="<%= producto.stock %>" class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-indigo-400" required>
                </div>
            </div>

            <div>
                <label class="block font-semibold text-gray-700">Imagen (URL)</label>
                <input type="text" name="imagen" value="<%= producto.imagen %>" class="w-full border rounded-lg px-4 py-2 focus:ring-2 focus:ring-indigo-400" required>
            </div>

            <div class="flex justify-end space-x-4">
                <a href="/" class="bg-gray-500 hover:bg-gray-600 text-white px-5 py-2 rounded-lg">Cancelar</a>
                <button type="submit" class="bg-indigo-600 hover:bg-indigo-700 text-white px-5 py-2 rounded-lg">Actualizar</button>
            </div>
        </form>
    </main>

</body>
</html>
